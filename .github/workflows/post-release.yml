name: Post-Release Sync

on:
  push:
    branches: [main]

jobs:
  sync:
    uses: Coongro/plugin-ci/.github/workflows/plugin-post-release.yml@main
    secrets: inherit
    permissions:
      contents: write
      pull-requests: write
